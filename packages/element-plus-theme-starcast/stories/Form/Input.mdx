import { Meta } from '@storybook/blocks';
import InputStories from './Input.stories';

<Meta of={InputStories} />

* 出于交互设定原因，`ElInput` 不使用原生自带的 `show-password` 属性，而是使用 `suffix slot` 自行实现
* 如果使用 `textarea` 且需要开启 `autosize`，则根据设计规范，一般情况下固定使用以下设定

```
:autosize="{
  minRows: 3,
  maxRows: 6
}
```
