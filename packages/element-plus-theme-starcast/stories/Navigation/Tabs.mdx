import { Meta } from '@storybook/blocks';
import * as TabStories from './Tabs.stories'

<Meta of={TabStories} />

## 参数说明

### `type`
当前设计规范，仅使用了缺省值（`undefined`）：


### `tabPosition`

* 当前设计规范，只定义了 `top`
* 其他选项，做了样式不错位的兼容，但很少使用